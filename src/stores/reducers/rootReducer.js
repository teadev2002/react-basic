const initStae = {
  user: [
    { id: 1, name: "The anh" },
    { id: 2, name: "ANh HOang" },
  ],
};
const rootReducer = (state = initStae, action) => {
  return state;
};

export default rootReducer;
